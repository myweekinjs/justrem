import styled from "@emotion/styled";

const Logo = styled.h1`
  color: #1a202c;
  font-size: 28px;
  margin: 1rem 0 2rem;
  line-height: 1;
  transform: skew(-15deg);
  text-align: center;
`;

export default Logo;
